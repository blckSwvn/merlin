# TODO

# Current
replace hardcoded path manipulation that uses non porable stuff 
improve autocomplete:
    resolve ~ as /home/user
    unify file path more
improve cmdline by having history
have local cliboard be global for the editor rather than view specfic

---

### maybe/unclear
marks
regex/fuzzy finding
sessions
registers
